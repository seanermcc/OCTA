# Execution prompt: Stage A control/non-CNV model

Work in G:\OCT_TreeShrew\octa. Execute Stage A of outputs/auto_seg_8layer_v2/STAGED_DEVELOPMENT_NEXT_STEPS.md: build, train, evaluate and package a small eight-boundary U-Net for controls and suitable non-CNV retinal tissue. My broad manual review is complete. Proceed with implementation and a real training pilot; do not stop after writing another plan.

The latest instruction is staged development. Do not make the CNV-specific local-visibility/provenance GUI update or CNV specialist a prerequisite. Do not execute the older UNET_CNV_EXECUTION_PROMPT.md wholesale. Read the new summary, the dated census, original UNET_PLAN.md and UNET_CNV_REVIEW.md for background. Preserve historical plans and outputs; write a versioned Stage A implementation plan alongside them that clearly states this precedence.

Follow AGENTS.md, README.md, PIPELINE.md and the segmentation skill, respecting the active eight-boundary contract and current user instructions where historical advice differs. Activate conda octa before Python; inspect GPU/environment availability and use the supported official PyTorch installation instructions if dependencies are needed. Preserve the working scientific environment, recording dependency changes. Do not reconstruct RAW or write to raw/MATLAB directories. Never write or migrate human labels through scripts. Keep the older ten-surface workflow and annotations intact.

1. Refresh the census and build a versioned training manifest. On 2026-09-08 the standard queue had 160 decisions: 101 corrected, 59 rejected, across 32 volumes; corrected evidence existed in nine animals. These are not all eligible control examples. Join human surface validity, image exclusions, independently reviewed CNV footprints, source geometry, verified timing and acquisition QC. Separate true controls, footprint-negative post-laser tissue, remote tissue from positive volumes and unknown status. Never use a selection-role flag or nominal D0 alone as biological control evidence. Missing masks are unknown, not negative. Exclude lesion and a documented physical surrounding buffer from Stage A supervision and validated export. State and audit the buffer convention; outside it is not automatically healthy. Report eligible supervision by surface, animal and QC group before selecting splits.

2. Use existing labels without fabricating provenance. Only corrected, edited, visible and reliable evidence outside exclusions is eligible; displaced-only and untouched automatic surfaces are not ground truth. Local stroke history is absent in legacy files. Document the limitation and audit enough existing evidence to justify a conservative policy; do not derive human drawing history from surface differences or demand wholesale relabeling. If a small human check is essential, prepare specific examples and continue independent work. Region targets require valid bounding surfaces. Keep rejected images for appropriately interpreted rejection evaluation. Do not invent local visibility targets from global rejections.

3. Repair the evaluation and freeze reproducible animal splits. Preserve a versioned original regression-test cohort separately from the completed label set. Add column-level median/tail boundary error, signed thickness bias and absolute error, gross-error frequency and contiguous extent, per-animal/QC results and risk-versus-coverage. Detect a synthetic 100 micrometer error confined to roughly 10% of columns. Count missing/NaN predictions explicitly; report raw and retained-prediction errors with denominators. Use animal-grouped development validation and a locked test if coverage permits, or nested grouped evaluation if necessary. Never separate an animal's eyes, dates or repeats. Do not tune or calibrate on test animals. Save manifests/hashes and split rationale, including the limitation of one WT animal and uneven animal representation. Do not silently reshuffle on new labels.

4. Implement a small 2-D boundary U-Net, masked losses, training, checkpointing, evaluation and source-coordinate inference. Retain all eight current targets and derived layers. Use canonical structural images, adequate crop containment and reversible geometry. Start with N=1; compare N=3 only if a small development experiment warrants it, preserving train/inference consistency. Do not inherit spatially mismatched flattened/unflattened channels. Keep source reads efficient and caches fingerprinted by source/geometry/preprocessing, with explicit completeness checks. Maintain thin-layer resolution and realistic augmentations. Do not train confident reconstruction targets under fully obscuring synthetic shadows. Optional region/measurability heads require valid supporting targets. Any ordering decoder must be evaluated for displacement and must not impose normal thickness as ground truth.

5. Run meaningful tests and a real pilot on the eligible data, then finish development evaluation. Compare the v2 baseline on identical examples and document its historical development overlap. Use animal-aware sampling/reporting so TS247/TS267 do not determine the entire result. Calibrate uncertainty/review thresholds on development data; entropy alone is not reliable confidence. Define acceptance criteria before final-test evaluation, marking them provisional if repeatability/scientific tolerances are unavailable. Prepare a small blind-repeat queue if useful without blocking software or pilot progress. Expand to neighboring-B-scan inputs only if the observed failure analysis justifies it; avoid an open-ended architecture sweep.

6. Deliver reproducible train/evaluate/infer commands, model and configuration artifacts, before/after overlays on held-out examples, per-surface/layer metrics, bias and coverage, gross-failure counts, runtime and a clear promotion decision. Implement resumable volume inference with original-coordinate surfaces, reason-coded validity, NaN thicknesses and complete provenance. Verify full-volume inference before batch use. Restrict validated Stage A exports to eligible control/non-CNV regions; mask CNV/buffer regions and record unknown eligibility explicitly. Keep experimental predictions distinguishable from validated measurements. If the candidate does not meet the declared criteria, retain it as experimental and implement the smallest evidence-supported next fix rather than claiming production readiness.

Complete the Stage A work as far as actual data and required scientific decisions allow. Report any narrow human-label dependencies explicitly, without treating the unfinished CNV-specific workflow as a blocker. Do not train a CNV specialist in this task. Preserve the accepted Stage A model so later specialist development cannot silently degrade control performance.

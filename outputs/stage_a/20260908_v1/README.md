# Superseded preparation audit

This directory preserves the initial environment inventory, PyTorch installation plan/report, and the first audit attempt. That attempt deliberately stopped on off-review-image targets. Its incomplete manifest is **not a training dataset**.

Use `../20260908_v2/` instead. Version 2 includes all 160 decisions and explicitly excludes the 628 otherwise-eligible boundary-column targets outside the human review image. No human labels were changed.
